const chai = require('chai');
const { expect } = chai.expect;
const { processRelationsSimple, storeIt } = require('../src/couchdb_processing');

describe('couchdb processing', function() {
    it('simple relations processing', function() {
    })

    it('store processing', function() {
    })
})
